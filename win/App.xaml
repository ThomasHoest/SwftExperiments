<Application x:Class="SwftExperiments.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:SwftExperiments.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <conv:StringToVisibilityConverter    x:Key="StringToVisibility"/>
        <BooleanToVisibilityConverter        x:Key="BoolToVisibility"/>

        <!-- ── Colours ─────────────────────────────────────────────────── -->
        <Color x:Key="BgColor">#FF080808</Color>
        <Color x:Key="TextColor">#FFE8E3DA</Color>
        <Color x:Key="MutedColor">#FF888888</Color>
        <Color x:Key="AccentColor">#FFC4A07A</Color>
        <Color x:Key="CardBgColor">#FF0D0D0D</Color>
        <Color x:Key="CardBorderColor">#FF2A2A2A</Color>

        <SolidColorBrush x:Key="BgBrush"          Color="{StaticResource BgColor}"/>
        <SolidColorBrush x:Key="TextBrush"         Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="MutedBrush"        Color="{StaticResource MutedColor}"/>
        <SolidColorBrush x:Key="AccentBrush"       Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="CardBgBrush"       Color="{StaticResource CardBgColor}"/>
        <SolidColorBrush x:Key="CardBorderBrush"   Color="{StaticResource CardBorderColor}"/>

        <!-- ── Typography ──────────────────────────────────────────────── -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Light"/>
        </Style>

    </Application.Resources>
</Application>
