# Neon serverless Postgres — West Europe (aws-eu-central-1)
DATABASE_URL=

# Static API key for iOS telemetry ingest (generate with: openssl rand -hex 32)
TELEMETRY_API_KEY=

# Optional: previous key during rotation window
TELEMETRY_API_KEY_PREVIOUS=
